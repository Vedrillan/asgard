{% extends 'AsgardPlannerBundle::layout.html.twig' %}
{% block body %}
<h1>Game creation</h1>

<form action="{{ path('admin_game_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_game') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}